apiVersion: k3d.io/v1alpha4
kind: Simple
network: kube_polycode_staging
options:
  k3s:
    extraArgs:
      - arg: --tls-san=https://staging.polycode.do-2021.fr
        nodeFilters:
          - servers:*
      - arg: --disable=traefik
        nodeFilters:
          - servers:*
