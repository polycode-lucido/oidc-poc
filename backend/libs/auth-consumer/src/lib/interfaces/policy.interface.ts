import {
  Operator as CaslOperator,
  Action as CaslAction,
  Policies,
  Policy,
} from '@polycode/casl';

export type Operator = CaslOperator;
export type Action = CaslAction;
export type IRolePolicy = Policy;
export type IRolePolicies = Policies;
