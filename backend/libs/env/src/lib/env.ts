/* This is a TypeScript enum that is used to validate the `NODE_ENV` property of the
`EnvironmentVariables` class. */
export enum Environment {
  PRODUCTION = 'production',
  DEVELOPMENT = 'development',
}
